while (true) {
    Sas::UpdateSas(CollisionComponents, TransformComponents);

    Physics::HandleCollisions(CollisionComponents, TransformComponents, RigidbodyComponents);
    Physics::HandleConstraints(ConstraintComponents, TransformComponents, RigidbodyComponents);
    Physics::MoveRigidbodies(TransformComponents, RigidbodyComponents);

    Graphics::AddCachedMeshes(RenderComponents);
    Graphics::UpdateMeshTransforms(RenderComponents, TransformComponents);
    Graphics::Draw(RenderComponents);

    Scripts::DoWhatever(GameObjects);    
}